# Project harness entry

Read the nearest manifest, refresh actual Git state, then use `specs/` as product meaning and `contracts/` as behavioral obligations. Audit context again after compression or repeated questions. Use TDD and check semantic ownership before implementation.
