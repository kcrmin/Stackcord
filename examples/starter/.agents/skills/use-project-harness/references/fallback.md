# Fallback

Read `AGENTS.md` if present, `.harness/entry.md`, the manifest, profile, related specs, contracts, task state, and tests. Inspect actual Git and submodule state without mutation. Separate facts, stale state, unknowns, blockers, and ownership, then state one safe next action. Do not claim fingerprint, semantic-conflict, or release-identity checks passed without the CLI.
