# Project harness entry

Read the nearest manifest, refresh root and exact child repository state, then use `specs/` as product meaning and `contracts/` as behavioral obligations. Audit context after compression. Integrate compatible child commits before updating the root pointer.
