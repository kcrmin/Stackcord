# Fallback

Read `.harness/entry.md`, the manifest, profile, workspaces, related specs, contracts, and tests. Inspect actual root and child Git state without mutation. Separate facts, pointer mismatches, stale state, unknowns, blockers, and ownership. Do not claim fingerprint, semantic-conflict, or release-identity checks passed without the CLI.
