# Multi-repository example

This fixture declares two submodule workspaces without creating fake Git repositories. In a real initialized project, Git records exact gitlink commits and `.gitmodules`; context and conflict checks bind the same contract to provider and consumer work.
