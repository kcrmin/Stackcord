# Agent entry

<!-- orchestrator:begin -->
Before changing the project, resolve the orchestration root from actual Git state, read `.harness/entry.md`, and refresh context. Product meaning lives in `specs/`; obligations live in `contracts/`; coordination state lives in `.harness/`.
<!-- orchestrator:end -->
